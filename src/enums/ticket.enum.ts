export enum TicketType {
  INCIDENT = 'incident',
  DEMANDE = 'demande',
}

export enum TicketStatus {
  NOUVEAU = 'nouveau',
  EN_COURS = 'en_cours',
  RESOLU = 'resolu',
}

export enum TicketPriority {
  FAIBLE = 'faible',
  MOYENNE = 'moyenne',
  HAUTE = 'haute',
}
